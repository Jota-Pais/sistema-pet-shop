# Conexão com o banco PostgreSQL (ex: Supabase, Neon ou Postgres local)
# Usada pelo Prisma (prisma/schema.prisma) e pela aplicação.
DATABASE_URL="postgresql://usuario:senha@host:5432/nome_do_banco"

# Porta do servidor (opcional — padrão 3333)
PORT=3333
